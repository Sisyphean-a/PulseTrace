{
  "name": "pulsetrace",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "vitest run --globals --pool=threads --poolOptions.threads.singleThread",
    "test:ci": "vitest run --globals --pool=threads --poolOptions.threads.singleThread"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
